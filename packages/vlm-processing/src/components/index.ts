/**
 * Svelte components for VLM visualization and interaction
 *
 * Import components directly in Svelte applications:
 *
 * import VLMTableView from '@sden99/vlm-processing/components/VLMTableView.svelte';
 * import StratificationTree from '@sden99/vlm-processing/components/StratificationTree.svelte';
 * import ProcessingProgressBar from '@sden99/vlm-processing/components/ProcessingProgressBar.svelte';
 *
 * For VLM UI components, use @sden99/vlm-components package instead.
 */

// Component types and interfaces
export type {
	VLMTableProps,
	StratificationTreeProps,
	AnalysisChartProps,
	HierarchyViewerProps,
	ProgressBarProps
} from './types.js';
